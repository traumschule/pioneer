/**
 * Placeholder module for the legacy Bond modal. The original implementation was
 * removed during the staking dashboard revamp. Kept empty so that existing
 * imports compiled into older bundles do not break the TypeScript build while
 * the new staking flows are still under construction.
 */
export {}
